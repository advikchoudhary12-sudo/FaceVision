# For a GPU setup, use .\setup_gpu.ps1 rather than installing this file
# directly: InsightFace declares the CPU runtime as a dependency.
insightface>=0.7.3
numpy>=1.24
onnxruntime-gpu[cuda,cudnn]>=1.27
opencv-python>=4.8
customtkinter>=6.0
